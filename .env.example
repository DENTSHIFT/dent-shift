# 開発用(SQLite)。ARCHITECTURE.mdのとおり本番はPostgreSQL(Neon/Supabase)に切り替える
DATABASE_URL="file:./dev.db"

# P0では未使用(mock providerのみ)。実プロバイダー接続時に追加する
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# RESEND_API_KEY=
