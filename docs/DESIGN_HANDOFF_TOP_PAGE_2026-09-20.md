# DENT SHIFT — トップページ(LP) 軽微なUI調整 引き継ぎ指示書

作成日: 2026-09-20。作業対象: トップページのみ(`/`、`src/app/page.tsx`)。`/onboarding`・`/plans`は確認済みで既に十分整っているため対象外(本書5章参照)。

宛先: この指示書を渡された別のAIコーディングエージェント(GPT/Codex等)。過去4回、同種のUI調整を実施済み(診断結果画面: `0e25313`、ダッシュボード: `6e850af`、opsポータル: `793da9a`、認証3画面: `2c59216`)。

## 1. 最重要事項: これはLPの全面刷新ではない。軽微な見た目調整のみ

`src/app/page.tsx`には以下のコメントが既に残っている。

> 「2026-09-06のユーザー指示: トップページの文字表示「DENT SHIFT」を正式ロゴ画像へ差し替える(LP全面刷新は今回のスコープ外。ロゴ置換のみの最小変更)。」

この過去の決定を今回も踏襲する。**`design/reference/lp/`配下の2枚の画像を正本として全面的に模倣しないこと。** これらの画像は「AI集患SaaS」「AI集患ドック」という別ブランド名・実際とは異なる料金(29,800円/49,800円/79,800円 — 実際のDENT SHIFT価格は14,800円/39,800円/79,800円)・未実装の機能(競合スコア比較表、月次推移グラフ、レーダーチャート、説明動画、FAQアコーディオン等)を含む初期コンセプト案であり、現在の実装に対する正本ではない。

**今回やってよいこと(スコープ)**:
- ページ全体を、ログイン画面等(`src/app/auth.module.css`)や医院向けダッシュボード(`src/app/dashboard/dashboard.module.css`)で確立済みの配色・タイポグラフィ・余白の感覚に合わせて軽く整える。
- 見出し・本文・CTAボタン・補足文の**文言は一切変更しない**(既存コピーは`docs/PRODUCT_SPEC.md`・`DENT_SHIFT_CLAUDE_CODE_HANDOFF.md`の正本文言をそのまま使っているため)。
- レイアウトの余白・ボタンサイズ・タイポグラフィの階調など、見た目の質を底上げする程度の調整。

**今回やってはいけないこと**:
- ヒーローセクション以外の新セクション追加(課題・お悩み、サービスでできること、診断の流れ、競合比較、説明動画、FAQ、料金プラン表、フッター等)。これらはLP全面刷新に該当し、スコープ外。
- ブランド名表記の変更(「DENT SHIFT」以外の名称を使わない)。
- 料金・プラン内容の記載追加(料金は`/plans`ページの担当であり、トップページには詳細を書かない、という現状の構成を維持する)。
- 実装されていない機能(競合スコア比較、AIレーダーチャート等)の紹介。

## 2. 現状の実装ファイル

- `src/app/page.tsx`(唯一のファイル。インラインstyle、CSS Modules未使用)

## 3. 現状の内容(変更しない要素)

- ロゴ画像(`/brand/logo/DENT_SHIFT_horizontal_tagline_transparent.png`)
- 見出し: 「歯科集患を、AIでシフトする。」
- 説明文: 「現状が分かる。競合との差が分かる。次に何をすればよいか分かる。予約につながったかまで分かる。」
- 主CTA: 「無料でAI集患診断する」(`/diagnosis`)
- 補足: 「営業電話なし ・ 約60秒 ・ クレジットカード不要 ・ まず結果だけ確認」
- 副リンク: 「プランの機能を比較する」(`/plans`)

これらの文言・リンク先はすべて維持し、見た目(配置・余白・フォントサイズ・装飾)のみ調整すること。

## 4. 見た目の調整方針(参考、必須ではない)

- 中央寄せの素朴なテキストレイアウトから、他ページと統一感のあるカード風または余白の取れた構成に調整してもよい。
- ボタンのスタイルを`src/app/auth.module.css`の`.primaryButton`相当に近づける。
- モバイル表示を確認すること(現状は`maxWidth: 640`で概ね問題ないはずだが、必ず実機確認すること)。

## 5. `/onboarding`・`/plans`について(参考情報、対応不要)

2026-09-20の実機確認で、両画面とも既にブランドヘッダー・カードレイアウト・進捗表示・バッジ等が整った状態であることを確認済み。**今回これらへの追加対応は不要。** もし触るとしても、本書の対象外なので別タスクとして扱うこと。

## 6. 厳守事項

- `src/server/**`・`src/domain/**`・Prisma schema・migrationは一切変更しないこと。
- リンク先(`/diagnosis`、`/plans`)を変更しないこと。
- 新しいnpmパッケージを追加しないこと。
- ブランドロゴの再生成・変形・色変更をしないこと。
- 見出し・本文・CTA文言・補足文言を一切変更しないこと(1章参照)。

## 7. 動作確認の手順

```bash
cd /Users/masatokimura/Documents/dent-shift
npm run dev
```

**注意**: `localhost:3000`が過去に別プロジェクトのdevサーバーと衝突したことがある。`http://127.0.0.1:3000`のようにIPv4を明示してアクセスするか、事前に`lsof -nP -iTCP:3000 -sTCP:LISTEN`で確認すること。`pkill`で無関係なプロセスを巻き込まないよう注意。

トップページはログイン不要でそのまま確認できる。テストデータの作成は不要。

## 8. 完了報告に含めること

- 作業完了時に必ず以下をすべて成功させること:
  ```bash
  npm run test
  npx tsc --noEmit -p tsconfig.json
  npm run lint
  npm run build
  ```
- 変更前後のスクリーンショット(PC・モバイル)
- 変更ファイル一覧・差分要約
- 文言・リンク先が一切変更されていないことの明示的な確認
- 新セクションを追加していないことの明示的な確認
